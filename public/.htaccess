<IfModule mod_rewrite.c>
    RewriteEngine On

    # Prevent Apache from serving directory listings — route through front controller
    DirectorySlash Off
    DirectoryIndex disabled

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
